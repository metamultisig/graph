{
  "name": "multisig",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ radek1st/multisig",
    "create-local": "graph create --node http://localhost:8020/ radek1st/multisig",
    "remove-local": "graph remove --node http://localhost:8020/ radek1st/multisig",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 radek1st/multisig"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.11.2",
    "@graphprotocol/graph-ts": "0.11.0"
  }
}
